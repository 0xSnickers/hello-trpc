import { trpc } from '../utils/trpc';

export default function TestPage() {
  return (
    <div>
      <h1>tRPC Test Page</h1>
      <p>This is a simple test page to verify tRPC configuration.</p>
    </div>
  );
}
